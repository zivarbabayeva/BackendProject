@model Blog
@{
    ViewData["Title"] = "Detail";
}

<div class="Raw">
    <div class="col-12">
        <h1>@Model.Title</h1>
        <div>
            @Html.Raw(Model.Date)
        </div>
        <div>
            <a asp-action="Index" class="btn btn-inverse-secondary">Go Back</a>
        </div>
    </div>
</div>
